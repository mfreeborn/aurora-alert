#[test]
fn test_register_workflow() {
    // 1. Submit form as a new, unregistered user
    // 2. Check email sent
    // 3. Check unverified user exists in database
    // 4. Use `verify` url from the email
    // 5. Check user is now verified in teh database
}
